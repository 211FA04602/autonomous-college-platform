// identity-access: NOT implemented in this foundation prompt beyond an empty,
// compiling module skeleton. See README.md.

dependencies {
    implementation(project(":platform-core"))

    testImplementation("org.springframework.boot:spring-boot-starter-test")
}
