/**
 * Authentication, authorization, roles, memberships, and sessions (PRD §7).
 *
 * <p><strong>Not implemented in this foundation prompt.</strong> This package exists only so the
 * module graph and ArchUnit boundary tests are complete and testable now
 * (docs/architecture/MODULE_BOUNDARIES.md). See README.md in this module for what is deferred and
 * why — do not treat any class here as a working feature (constitution rule 13).
 */
package com.acplatform.identityaccess;
