{
  "name": "@acplatform/validation-schemas",
  "version": "0.1.0",
  "private": true,
  "description": "Shared Zod validation schemas mirroring backend API contracts. Business-domain schemas are added alongside their owning module, not speculatively.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "echo \"no tests yet for validation-schemas\" && exit 0"
  },
  "dependencies": {
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
