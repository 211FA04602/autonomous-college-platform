{
  "name": "@acplatform/design-tokens",
  "version": "0.1.0",
  "private": true,
  "description": "Shared design tokens (color, spacing, typography, radii) consumed by frontend and mobile.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "echo \"no tests yet for design-tokens\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
