{
  "name": "@acplatform/i18n-resources",
  "version": "0.1.0",
  "private": true,
  "description": "Shared localization resource bundles and extraction contract for frontend and mobile. Only the 'common' and 'systemHealth' namespaces are populated in this foundation; per-role/business namespaces are added with their owning feature.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "echo \"no tests yet for i18n-resources\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
