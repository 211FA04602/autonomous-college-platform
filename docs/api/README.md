# API documentation

`openapi.yaml` describes the public REST API, versioned (`/v1/...`). It is the source of truth for the contract `packages/api-contracts` mirrors on the client side (constitution rule 5). New endpoints are documented here in the same change that adds them — an undocumented endpoint is not considered complete.
