# AWS infrastructure (placeholder)

No Terraform is implemented yet. See ADR-006 for direction (ECS Fargate, Aurora PostgreSQL, S3, Secrets Manager). This directory is created now so the repository structure is complete; actual modules are added once there is a real deployable artifact from `backend/` and `frontend/` to target.
